import React from 'react';

// FAQPage removed from navigation — placeholder kept to avoid import errors during development
export default function FAQPage() {
  return <div style={{ padding: 24 }}>FAQ page removed (unused).</div>;
}

